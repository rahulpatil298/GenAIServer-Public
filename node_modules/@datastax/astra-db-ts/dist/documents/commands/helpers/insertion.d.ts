import { DataAPIHttpClient } from '../../../lib/api/clients';
import { SerDes } from '../../../lib/api/ser-des/ser-des';
import { DataAPIDetailedErrorDescriptor, DataAPIResponseError } from '../../../documents/errors';
import { SomeDoc } from '../../../documents';
import { TimeoutManager } from '../../../lib/api/timeouts';
/**
 * @internal
 */
export declare const insertManyOrdered: <ID>(httpClient: DataAPIHttpClient, serdes: SerDes, documents: readonly SomeDoc[], chunkSize: number, timeoutManager: TimeoutManager, err: new (descs: DataAPIDetailedErrorDescriptor[]) => DataAPIResponseError) => Promise<ID[]>;
/**
 * @internal
 */
export declare const insertManyUnordered: <ID>(httpClient: DataAPIHttpClient, serdes: SerDes, documents: readonly SomeDoc[], concurrency: number, chunkSize: number, timeoutManager: TimeoutManager, err: new (descs: DataAPIDetailedErrorDescriptor[]) => DataAPIResponseError) => Promise<ID[]>;
