"use strict";
// Copyright Datastax, Inc
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
exports.TokenProvider = void 0;
const lib_1 = require("../../lib");
const utils_1 = require("../../lib/utils");
class TokenProvider {
        static mergeTokens(...raw) {
        const first = raw.find((r) => !(0, utils_1.isNullish)(r));
        if (typeof first === 'string') {
            return new lib_1.StaticTokenProvider(first);
        }
        if (!(first instanceof TokenProvider) && !(0, utils_1.isNullish)(first)) {
            throw new TypeError(`Expected token to be of type string | TokenProvider | nullish; got ${typeof first} (${first})`);
        }
        return first;
    }
    ;
}
exports.TokenProvider = TokenProvider;
