import { DataAPILoggingOutput } from '../../lib/logging/types';
/**
 * @internal
 */
export declare const buildOutputsMap: (emits: readonly DataAPILoggingOutput[]) => {
    event: boolean;
    stdout: boolean;
    stderr: boolean;
} | undefined;
